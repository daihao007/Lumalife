apiVersion: v1
kind: Service
metadata:
  name: assistant-service
  namespace: lumalife
  labels:
    app.kubernetes.io/name: assistant-service
    app.kubernetes.io/part-of: lumalife
    app.kubernetes.io/version: "0.1.0"
spec:
  selector:
    app.kubernetes.io/name: assistant-service
  ports:
    - name: http
      port: 8084
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: assistant-service
  namespace: lumalife
  labels:
    app.kubernetes.io/name: assistant-service
    app.kubernetes.io/part-of: lumalife
    app.kubernetes.io/version: "0.1.0"
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: assistant-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: assistant-service
        app.kubernetes.io/part-of: lumalife
        app.kubernetes.io/version: "0.1.0"
    spec:
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: assistant-service
          image: ghcr.io/daihao007/lumalife-assistant-service:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8084
          env:
            - name: SERVICE_VERSION
              value: "0.1.0"
            - name: LUMALIFE_INTERNAL_SERVICE_TOKEN
              value: compose-internal-token
            - name: AGNES_ENDPOINT
              value: https://apihub.agnes-ai.com/v1/chat/completions
            - name: AGNES_MODEL
              value: agnes-2.0-flash
            - name: AGNES_API_KEY
              valueFrom:
                secretKeyRef:
                  name: lumalife-ai
                  key: api-key
                  optional: true
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 5
            failureThreshold: 24
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 384Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
