apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Ephemeral Kind smoke-test fixtures only. Production uses k8s/services.yaml.
resources:
  - identity-service.yaml
  - merchant-service.yaml
  - order-service.yaml
  - assistant-service.yaml
