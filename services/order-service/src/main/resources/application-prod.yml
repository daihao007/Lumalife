spring:
  datasource:
    url: jdbc:mysql://${MYSQL_ORDER_HOST:${MYSQL_HOST:mysql}}:${MYSQL_ORDER_PORT:${MYSQL_PORT:3306}}/${MYSQL_DATABASE}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${MYSQL_USER:lifeassist}
    password: ${MYSQL_PASSWORD:}
