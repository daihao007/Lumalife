server:
  port: ${LUMALIFE_ORDER_PORT:8083}
spring:
  application:
    name: order-service
management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    env:
      enabled: true
info:
  service:
    name: order-service
    version: ${SERVICE_VERSION:0.1.0}
    contract-version: v1
