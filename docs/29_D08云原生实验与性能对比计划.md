# D08 云原生实验与性能对比交付计划

> 当前收口状态（2026-09-02）：性能已有真实三 API × 两模式 × 三重复原始结果；性能 workflow 的当前本地修复已加入矩阵/artifact 硬校验，但未在本地伪造 GitHub Actions 成功。merchant-service HPA 已在远端 K3s 确认 `metrics.k8s.io` 可用，并真实观察到 1→2→3→1，当前为 PASS。统一证据入口见 [`04_tests/evidence/README.md`](../04_tests/evidence/README.md)。

## 1. 对照课程评分项

| 评分项 | 仓库资产 | 验收动作 | 当前状态 |
| --- | --- | --- | --- |
| 自动扩缩容 3 分 | `k8s/hpa.yaml`、`04_tests/cloud-native/run-hpa-experiment.sh` | 先确认/安装适配当前集群的 metrics-server，再以 merchant-service 为目标完成 1→2/3→1 负载和冷却观测 | **PASS**：远端 K3s 已有 `kube-system/metrics-server`，真实观察到 merchant-service Ready 与 HPA current/desired `1→2→3→1`；详见 [`HPA_EXPERIMENT_REPORT.md`](HPA_EXPERIMENT_REPORT.md) |
| 故障处理 2 分 | BFF 远程适配器的超时/503 错误外壳、CI failure-injection、三容器服务隔离 | 停止 merchant-service 或注入延迟，记录用户友好的 503，确认其他服务健康 | 已完成 Docker Desktop Kubernetes 实测，before/during/after 均有原始证据 |
| 性能对比 4 分 | `04_tests/performance/load-test.mjs`、`04_tests/performance/run-comparison.sh`、结果 artifact | 相同机器/数据/脚本分别跑 monolith 与 microservices，各 3 次，保存 JSON/CSV 和 CPU/内存 | **本地实测 PASS**：三 API、两模式、三重复，原始 JSON/CSV 和 Docker stats 已保存；workflow 另有 2×3 矩阵硬校验，远端当前最新记录仍是旧版失败 |

## 2. HPA 实验

生产清单对 backend 和 merchant-service 设置 CPU request、`minReplicas=1`、`maxReplicas=3` 和 60% CPU 目标；扩容立即生效，缩容有 120 秒稳定窗口，避免短时抖动。目标集群需要提供 Resource Metrics API（通常由 metrics-server 提供），否则 HPA 只能创建但不能根据 CPU 调整副本数。本轮实验优先 merchant-service，远端 K3s 实测已完成并保存原始证据。

```bash
kubectl -n lumalife get apiservice v1beta1.metrics.k8s.io
kubectl -n lumalife get hpa merchant-service
NAMESPACE=lumalife LOAD_SECONDS=180 COOLDOWN_SECONDS=180 \
  bash 04_tests/cloud-native/run-hpa-experiment.sh
```

脚本输出 timestamp、副本/Ready Pod、HPA 当前/期望值、CPU/内存、请求数、吞吐、平均延迟、P95 和错误率；原始 CSV、负载日志、`kubectl top`、事件、服务日志及 HPA 清单要随报告提交。若 `metrics.k8s.io` 不可用，相关字段保留为 `N/A`，不得把实验写成成功扩容。安装 metrics-server 前必须先确认 Kubernetes 发行版和版本，不能在未知集群上盲目套用镜像版本；本次集群已有可用组件，因此未执行安装替换。

## 3. 故障处理实验

微服务之间只通过 HTTP 调用；BFF 对远程服务连接/读取失败映射为明确的 503 业务错误，不让异常堆栈泄漏给用户。实验步骤：

```bash
kubectl -n lumalife scale deployment/merchant-service --replicas=0
curl -i 'http://localhost:30080/api/v1/merchants?keyword=%E5%92%96%E5%95%A1'
kubectl -n lumalife get pods
kubectl -n lumalife scale deployment/merchant-service --replicas=1
```

应记录：商家查询得到预期不可用提示；identity/order/数据库等未被级联停止；恢复依赖服务后健康检查和查询恢复。线上演示使用隔离窗口或验收集群，不直接对无窗口生产流量注入故障。本轮实测证据见 `04_tests/cloud-native/fault/summary.md` 和 `docs/FAULT_TOLERANCE_EXPERIMENT_REPORT.md`。

## 4. 性能对比

正式实验必须固定：同一机器、同一批种子数据、同一个公开接口、同一并发和请求数。先以微服务开关全开运行，再以显式 `monolith` profile 运行单体兼容路径。`run-comparison.sh` 对商家搜索、分类列表、商家详情使用同一个 `load-test.mjs`，每边每个 API 运行 3 次，并保存吞吐量、平均耗时、P95、错误率和 backend Docker CPU/内存。

性能结果只能引用实际生成的 artifact/CSV。报告需要解释远程 HTTP、数据库访问和服务启动成本造成的差异，不能仅凭理论声称“微服务更快”。本轮实际结果见 [`04_tests/performance/results/nightly-20260902/comparison-summary.csv`](../04_tests/performance/results/nightly-20260902/comparison-summary.csv)；workflow 下载入口见 [Performance comparison workflow](https://github.com/daihao007/Lumalife/actions/workflows/performance.yml)，每次成功运行的 artifact 名称为 `performance-comparison-results`。
