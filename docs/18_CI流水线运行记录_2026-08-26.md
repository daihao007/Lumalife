# LumaLife 原系统 CI 流水线运行记录

> 记录日期：2026-08-26
>
> 记录生成时间：2026-08-26 16:59（Asia/Shanghai）
>
> 实践阶段：前 5 天——原系统整理、测试、容器化与 CI/CD
>
> 依据：《软件工程基础实践》课程任务书（2026 夏季学期）

## 1. 结论

本日保留了同一提交的成功、失败两条 GitHub Actions 原始记录：

- 成功运行完成取代码、依赖安装、编译、后端单元/集成测试、数据库迁移验证、CR-04～CR-06 API E2E、前端构建、版本化镜像制作和 GHCR 发布；
- 失败运行通过工作流提供的故障演示开关使质量门禁失败，后续镜像矩阵被标记为 `Skipped`，证明门禁失败后不会继续制作或发布镜像；
- 当前流水线尚未接入 Kubernetes 部署和部署后健康检查，因此只能判定为“原系统 CI 与镜像发布已跑通”，不能表述为课程要求的完整 CI/CD 已完成；
- 当前前端作业执行 `npm run build`，没有执行已经存在的 `npm test`；API E2E 仅覆盖 CR-04～CR-06，这两项仍是中期检查前的测试缺口。

## 2. 运行环境与配置

| 项目 | 实际配置 |
| --- | --- |
| 仓库 | `daihao007/Lumalife` |
| 工作流 | `Monolith CI`（`.github/workflows/ci.yml`） |
| 成功触发 | Push 到 `main` |
| 失败触发 | `workflow_dispatch`，`demonstrate_failure_gate=true` |
| Runner | GitHub-hosted `ubuntu-latest` |
| 后端 | Temurin Java 17、Maven，命令 `mvn -B -ntp verify` |
| 前端 | Node.js 22，命令 `npm ci --no-audit --no-fund`、`npm run build` |
| 数据库验证 | MySQL 8.4、Docker Compose |
| 镜像构建 | Docker Buildx、GHCR |
| 被测提交 | `0d1f69a6a6bd23bcfdc657b4d0f6d4251e71ee5e` |

仓库未在工作流或本记录中写入密码、Token 或数据库口令。数据库验证使用运行时随机凭据；GHCR 使用 GitHub 自动提供的 `GITHUB_TOKEN`。

## 3. 成功运行记录

| 字段 | 记录 |
| --- | --- |
| Run ID | [`32946166599`](https://github.com/daihao007/Lumalife/actions/runs/32946166599) |
| 事件 | `push` 到 `main` |
| 开始时间 | 2026-08-26 16:07:39（北京时间） |
| 结束时间 | 2026-08-26 16:09:22（北京时间） |
| 总耗时 | 1 分 43 秒 |
| 结论 | `success` |

### 3.1 作业结果

| 作业 | 结果 | 主要证据 |
| --- | --- | --- |
| Backend test | 成功 | Maven 编译、单元/集成测试通过；上传 `backend-test-reports` |
| Frontend build | 成功 | `npm ci` 和 TypeScript/Vite 构建通过；上传 `frontend-dist` |
| Database schema | 成功 | MySQL 启动；迁移与种子各重复执行两次；清理、重建和验证通过 |
| API E2E test | 成功 | CR-04、CR-05、CR-06 三个 API 业务场景通过；上传 `api-e2e-reports` |
| Quality gate | 成功 | 四个前置作业全部成功后放行 |
| Build backend image | 成功 | 构建并向 GHCR 推送 `main` 与 `sha-0d1f69a` |
| Build frontend image | 成功 | 构建并向 GHCR 推送 `main` 与 `sha-0d1f69a` |

## 4. 测试报告

### 4.1 后端 Maven 测试

以下数据来自成功 Run 的 `backend-test-reports` 原始 Surefire XML：

| 测试套件 | 类型 | 总数 | 通过 | 失败 | 错误 | 跳过 | 套件耗时 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `DemoStoreTest` | 单元/领域规则 | 41 | 41 | 0 | 0 | 0 | 24.162 s |
| `ApiSecurityIntegrationTest` | 集成/API | 25 | 25 | 0 | 0 | 0 | 27.883 s |
| `DatabaseAssetsTest` | 数据库资产校验 | 2 | 2 | 0 | 0 | 0 | 0.209 s |
| **后端合计** |  | **68** | **68** | **0** | **0** | **0** | **52.254 s** |

本次没有失败测试，因此失败原因为空。测试作业总运行时间高于套件耗时，因为还包含取代码、Java 环境配置、依赖解析和报告上传。

### 4.2 API E2E 测试

以下数据来自成功 Run 的 `api-e2e-reports/e2e-report.json`：

| 场景 | 结果 | 耗时 |
| --- | --- | ---: |
| CR-04 团购购买、券码核销与异常边界 | 通过 | 494 ms |
| CR-05 完成订单评价、评分校验与重复评价限制 | 通过 | 375 ms |
| CR-06 商家履约、商品/套餐校验与角色边界 | 通过 | 563 ms |
| **合计** | **3/3 通过，0 失败** | **1,432 ms** |

E2E 环境地址为作业内隔离启动的 `http://localhost:8080`，环境启动状态为 `passed`。后端 Maven 与 API E2E 合计执行 71 项自动化检查，全部通过。

### 4.3 前端验证边界

前端作业完成了依赖安装、TypeScript 检查和 Vite 生产构建，但工作流没有执行 `npm test`。因此本记录只把该作业记为“前端编译/构建通过”，不把仓库中的前端测试计入本次流水线测试总数。

## 5. 数据库迁移验证

成功运行使用随机生成的隔离凭据启动 MySQL 8.4，并依次执行：

1. 迁移两次；
2. 测试数据初始化两次；
3. 数据校验；
4. 数据清理；
5. 再次初始化并校验；
6. 无论成功或失败均删除测试数据库和卷。

该记录证明当前迁移、种子和清理脚本可重复执行。它不等同于 Kubernetes 中的数据库部署或线上迁移验证。

## 6. 版本化镜像记录

| 组件 | 镜像 | 版本标签 | Digest |
| --- | --- | --- | --- |
| 后端 | `ghcr.io/daihao007/lumalife-backend` | `sha-0d1f69a`、`main` | `sha256:735d749c7834cbd9a02cdc9926ab090750d35061fd293f9c4b5416887c9a5578` |
| 前端 | `ghcr.io/daihao007/lumalife-frontend` | `sha-0d1f69a`、`main` | `sha256:08ad74811fddceba7e7e24ac00bc1869b496848560c9d3971ea263d37beb3c20` |

标签和 Digest 均来自成功 Run 的 Docker Buildx 原始日志。镜像同时保留分支标签和不可变提交标签，没有只发布 `latest`。当前 GitHub CLI Token 缺少 `read:packages`，无法通过 Packages API 再次枚举版本；这不影响 Actions 日志中已确认的推送结果。

## 7. 失败门禁记录

| 字段 | 记录 |
| --- | --- |
| Run ID | [`32950190700`](https://github.com/daihao007/Lumalife/actions/runs/32950190700) |
| 事件 | 手动 `workflow_dispatch` |
| 输入 | `demonstrate_failure_gate=true` |
| 开始时间 | 2026-08-26 16:54:37（北京时间） |
| 结束时间 | 2026-08-26 16:55:56（北京时间） |
| 总耗时 | 1 分 19 秒 |
| 结论 | `failure`（预期失败） |

### 7.1 失败前后的执行结果

| 顺序 | 作业 | 结果 |
| ---: | --- | --- |
| 1 | Backend test | 成功 |
| 1 | Frontend build | 成功 |
| 1 | Database schema | 成功 |
| 1 | API E2E test | 成功 |
| 2 | Quality gate / `Demonstrate the failure gate` | **失败（故意注入）** |
| 3 | `Confirm validation passed` | `Skipped` |
| 4 | 后端/前端镜像矩阵 | **整体 `Skipped`** |

失败日志的明确错误为：

```text
Manual failure demonstration requested. Image publication must be skipped.
Process completed with exit code 1.
```

这是一条受控门禁失败记录，不是真实测试用例失败。它验证了 `images` 作业对 `quality-gate` 的依赖关系：质量门禁失败后不会执行镜像构建和发布。由于当前尚无 Kubernetes 作业，本次记录不能证明部署阻断或回滚。

## 8. 与课程任务书逐项对照

| 任务书要求 | 当前证据 | 状态 |
| --- | --- | --- |
| 取代码 | `actions/checkout` | 已完成 |
| 安装依赖 | Maven 缓存/解析、`npm ci` | 已完成 |
| 编译 | `mvn verify`、TypeScript/Vite | 已完成 |
| 单元测试 | `DemoStoreTest` 41/41 | 已完成 |
| 集成/API 测试 | `ApiSecurityIntegrationTest` 25/25 | 已完成 |
| 数据库脚本可重复运行 | Database schema 作业 | 已完成 |
| 端到端测试 | CR-04～CR-06 API E2E 3/3 | **部分完成：尚未覆盖全部确认用例** |
| 前端自动测试进入流水线 | 当前只执行 `npm run build` | **未完成** |
| 制作并发布版本镜像 | 两个 `sha-0d1f69a` 镜像及 Digest | 已完成 |
| 失败后停止后续发布 | 失败 Run 中镜像矩阵 `Skipped` | 已验证 |
| Kubernetes 部署 | 工作流无部署作业 | **未完成** |
| 部署后健康检查 | 工作流无健康检查作业 | **未完成** |
| 成功和失败记录都保留 | Run `32946166599`、`32950190700` | 已完成 |
| 测试报告含总数、通过、失败、原因和环境 | 本记录第 2、4、7 节 | 已完成 |

## 9. 中期检查前必须补齐

1. 在 `Frontend build` 中加入 `npm test`，测试失败必须阻断质量门禁；
2. 把确认后的全部业务场景纳入 E2E，而不是只保留 CR-04～CR-06；
3. 增加 Kubernetes 部署、就绪/存活检查及部署失败阻断；
4. 部署完成后记录 Pod、镜像版本、健康地址和检查结果；
5. 在 GitHub Artifact 7 天保留期结束前下载 `backend-test-reports`、`api-e2e-reports` 和 `frontend-dist`，归档到最终材料的 `03_devops`/`04_tests`；
6. 若后续修改工作流，重新各保留一次成功与失败 Run，保证记录对应最终提交。

## 10. 证据索引

- 成功流水线：<https://github.com/daihao007/Lumalife/actions/runs/32946166599>
- 失败门禁流水线：<https://github.com/daihao007/Lumalife/actions/runs/32950190700>
- 工作流配置：`.github/workflows/ci.yml`
- 流水线设计说明：`docs/15_原系统CI流水线说明.md`
- 数据库验证记录：`docs/evidence/2026-08-26/issue-25-database-validation.md`
- API E2E 执行说明：`docs/17_ISSUE-29_E2E执行记录_2026-08-26.md`

本文仅记录已由 GitHub Actions、测试报告和构建日志证明的结果；未接入或未实际运行的项目明确标为“部分完成”或“未完成”。
