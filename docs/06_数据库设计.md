# 数据库设计

当前版本使用内存数据。后续 MySQL 建议表：

- `user_account`
- `user_address`
- `merchant`
- `category`
- `product`
- `group_deal`
- `cart_item`
- `order_main`
- `order_item`
- `payment_record`
- `coupon`
- `review`
- `operation_log`

金额字段统一使用 `BIGINT`，单位为分。订单、支付、券码、评价表均保留 `created_at`、`updated_at` 和 `is_deleted`。

支付幂等索引：

```sql
UNIQUE KEY uk_payment_request (user_id, order_id, client_request_id)
```
