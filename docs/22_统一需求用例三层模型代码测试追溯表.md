# 统一需求—用例—三层模型—代码—测试追溯表

> 历史追溯快照：下文包含旧 SHA、78/78、27/27 等阶段数字，禁止作为当前测试口径。当前唯一追溯表见 [`final-audit/traceability-matrix.md`](final-audit/traceability-matrix.md)，测试数字见 [`testing/test-inventory.md`](testing/test-inventory.md)，关键事实见 [`project-facts.md`](project-facts.md)。

> 历史汇总基线：`main@bc36f3298b32dfa7f5961cae7bc3adc7c6059465`（2026-08-28）；当前 main 核对基线：`57c474b2e22778163a2ffc9daf12f900abf3ac37`（2026-09-02）；Microservice E2E 实测 run 使用提交 `4f22cc73c1edd5c52dbc7b75e2558fbf508a4cd2`
> 用途：将正式需求、业务用例、SYS/COMP/OBJ 三层模型、代码模块、测试编号和测试结果放在同一张可审查表中。
> 编号约定：正式需求使用 `REQ01~REQ09`，正式用例使用 `UC01~UC09`，测试用例使用 `TC-UCxx-nn`；`CR-01~CR-06` 仅作为历史核心范围映射。

> 当前实现说明：下方总表保留历史模型与主线测试汇总；其中 `DemoStore` 是 backend 的兼容 fallback，不代表服务没有实现。当前四服务业务 mapping 为 identity 13、merchant 30、order 20 + 1 个评价投影、assistant 1，共 65 个；逻辑数据表与远程切流以 [`project-facts.md`](project-facts.md) 和 [`微服务最终架构口径矩阵`](./MICROSERVICE_FINAL_ARCHITECTURE_MATRIX.md) 为准。

## D07 当前服务边界索引

| 需求/用例 | 当前服务/API 边界 | 当前代码事实 | 当前测试编号 |
|---|---|---|---|
| REQ01 / UC01 | `identity-service`；backend `RemoteIdentityServicePort` | 13 个 `/internal/v1/auth|users/**` mapping；生产 JDBC 写入 identity DB，JSON/内存仅为 smoke/fallback | CT-RT-ID-01~09；Microservice E2E UC01 |
| REQ02 / UC02 | `merchant-service` + backend BFF | 商家/商品/分类/筛选排序/收藏边界已纳入 merchant；评价只读通过 order projection | Microservice E2E UC02；merchant provider tests |
| REQ03~REQ07 / UC03~UC07 | `order-service`、`merchant-service` + BFF | 购物车、订单详情、支付、履约、评价、券码和商品管理已有内部接口；库存通过 RabbitMQ Outbox/Inbox/Saga 异步处理，释放状态明确 | Microservice E2E UC03~UC07；针对性 Saga 单测 |
| REQ08 / UC08 | `merchant-service` + `assistant-service` + BFF | merchant 持有会话事实，assistant-service 持有 provider/fallback；DemoStore 仅显式兼容路径 | Microservice E2E UC08 |
| REQ09 / UC09 | backend 管理门面 + 三服务只读投影/Actuator | `RemoteMetricsServicePort` 聚合 users/merchants/orders/amount；远程 UC09 已通过，merchant-service HPA 另有真实 1→2→3→1 集群 PASS 证据 | CT-RT-OPS-01~03；Microservice E2E UC09；HPA final acceptance |

## 统一追溯总表

| 需求（ID/目标） | 用例（ID/目标） | 三层模型（SYS / COMP / OBJ） | 代码模块、页面与接口 | 测试编号 | 测试结果 | 证据链接 |
| --- | --- | --- | --- | --- | --- | --- |
| `REQ01` 建立可登录、可下单的用户资料 | `UC01` 用户注册、登录、维护资料和地址 | `SYS-SEQ01` / `COMP-SEQ01` / `OBJ-SEQ01` | `AuthService`、`SecurityConfig`、`DemoStore`；`Login.tsx`、`Profile.tsx`；`/auth/**`、`/user/profile`、`/user/addresses/**` | `TC-UC01-01~17` | 当前 `main@bc36f32` 本地 Maven `78/78` 通过；登录/资料/地址及 401/403 证据已登记；注册→资料→地址浏览器 E2E 待补 | [D03 模型与矩阵](17_D03前半用例三层模型与追溯矩阵.md)；[测试报告](07_测试报告.md) |
| `REQ02` 发现商家并形成购买决策 | `UC02` 浏览、筛选、查看详情和收藏商家 | `SYS-SEQ02` / `COMP-SEQ02` / `OBJ-SEQ02` | `CatalogService`、`FavoriteService`、`DemoStore`；`Home.tsx`、`Detail.tsx`、`Favorites.tsx`；`/categories`、`/merchants/**`、`/user/favorites/**` | `TC-UC02-01~07` | 公共目录、筛选和收藏接口测试已执行，纳入当前 `main@bc36f32` Maven `78/78` 通过记录；收藏三处状态一致的浏览器 E2E 待补 | [D03 模型与矩阵](17_D03前半用例三层模型与追溯矩阵.md)；[业务用例清单](16_业务场景用例清单.md) |
| `REQ03` 提交外卖订单并得到支付或取消结果 | `UC03` 加购、拆单、下单、支付、取消和查看订单 | `SYS-SEQ03` / `COMP-SEQ03` / `OBJ-SEQ03` | `CartService`、`OrderWorkflowService`、`DemoStore`；`Detail.tsx`、`Cart.tsx`、`Orders.tsx`；`/cart/**`、`/orders/delivery`、`/payments`、`/orders/{id}/cancel` | `TC-UC03-01~18` | 购物车、库存、订单、支付幂等和取消测试纳入当前 `main@bc36f32` Maven `78/78` 通过记录；代表性 HTTP E2E 已通过，真实 UI E2E 待纳入持续流水线 | [D03 模型与矩阵](17_D03前半用例三层模型与追溯矩阵.md)；[D04 测试记录](18_ISSUE-34_E2E执行记录_2026-08-27.md) |
| `REQ04` 履约外卖订单、确认收货并评价 | `UC04` 商家履约、用户收货和评价 | `SYS-SEQ04` / `COMP-SEQ04` / `OBJ-SEQ04` | `MerchantAdminService`、`OrderWorkflowService`；`MerchantOrders.tsx`、`Orders.tsx`；`/merchant-admin/orders/**`、`/orders/{id}/receive`、`/reviews` | `TC-UC04-01~06`，以及 `TC-UC03-14` | 后端/API、跨角色 HTTP E2E 和页面异常断言已通过；当前 `main@bc36f32` Maven `78/78` 通过 | [D04 三层模型与审查](18_D04全部用例三层模型与中期追溯审查.md)；[D05 页面证据](evidence/D05/) |
| `REQ05` 购买团购套餐并获得券码 | `UC05` 购买团购、支付并取得可核销券码 | `SYS-SEQ05` / `COMP-SEQ05` / `OBJ-SEQ05` | `OrderWorkflowService`、`CatalogService`、`DemoStore`；`Detail.tsx`、`Orders.tsx`；`/orders/group-buy`、`/payments`、`/orders` | `TC-UC05-01~05` | 团购下单、库存、支付幂等和券码唯一性测试已执行；当前 `main@bc36f32` Maven `78/78` 通过，浏览器异常路径仍待补 | [D04 三层模型与审查](18_D04全部用例三层模型与中期追溯审查.md)；[D05 团购证据](evidence/D05/04-user-groupbuy-1020-paid-coupon.png) |
| `REQ06` 核销团购券并处理异常券码 | `UC06` 商家核销成功、重复、无效和越权券码 | `SYS-SEQ06` / `COMP-SEQ06` / `OBJ-SEQ06` | `MerchantAdminService`、`OrderWorkflowService`、`DemoStore`；`MerchantOrders.tsx`、`MerchantDesk.tsx`；`/merchant-admin/coupons/verify` | `TC-UC06-01~02`，以及 `TC-UC05-03` | 核销成功、重复、无效、未支付和跨商家边界测试已执行；当前 `main@bc36f32` Maven `78/78` 通过 | [D04 三层模型与审查](18_D04全部用例三层模型与中期追溯审查.md)；[D05 核销证据](evidence/D05/05-merchant-groupbuy-1020-coupon-entered.png) |
| `REQ07` 维护经营内容并发布到用户端 | `UC07` 编辑→发布→用户刷新→下架后不可购买 | `SYS-SEQ07` / `COMP-SEQ07` / `OBJ-SEQ07` | `MerchantAdminService`、`CatalogService`；`MerchantProducts.tsx`、`MerchantShop.tsx`、`Detail.tsx`；`/merchant-admin/profile`、`/products/**`、`/group-deals/**` | `TC-UC07-01~11`；`UC07PageFlow.test.tsx` | 页面闭环 `4/4` 断言通过；前端 Vitest 当前专项 `27/27` 通过；发布、刷新、下架和不可购买证据已留存 | [UC07 页面报告](evidence/D05/uc07-page-flow.md)；[原始日志](evidence/D05/uc07-page-flow.raw.log)；[页面测试](../frontend/src/pages/UC07PageFlow.test.tsx) |
| `REQ08` 完成用户、商家与 AI 客服沟通 | `UC08` 用户发送→商家读取→商家回复→用户再次读取 | `SYS-SEQ08` / `COMP-SEQ08` / `OBJ-SEQ08` | `AssistantService`、`DemoStore`；`Assistant.tsx`、`MerchantSupport.tsx`；`/conversations/**`、`/merchant-admin/conversations/**`、`/assistant/ask` | `TC-UC08-01~04`；UC08 HTTP E2E | UC08 API E2E `7/7` 通过；人工回复 `senderRole=MERCHANT`；商家 B 越权读取/回复均返回 `404`；浏览器级持续 E2E 另行规划 | [UC08 跨角色报告](evidence/D05/uc08-cross-role-log.md)；[PR #99](https://github.com/daihao007/Lumalife/pull/99) |
| `REQ09` 查看运营指标并判断系统状态 | `UC09` 平台管理员查看看板和健康状态 | `SYS-SEQ09` / `COMP-SEQ09` / `OBJ-SEQ09` | `AdminDashboardService`、`SecurityConfig`、`DemoStore`、Actuator `HealthEndpoint`；`Admin.tsx`；`/admin/metrics`、`/actuator/health` | `TC-UC09-01~04` | 管理员权限、指标持久化和 Actuator 健康链路测试已执行；当前 `main@bc36f32` Maven `78/78` 通过；真实健康聚合的后续拆分项仍单独跟踪 | [D04 三层模型与审查](18_D04全部用例三层模型与中期追溯审查.md)；[D05 管理看板证据](evidence/D05/08-admin-dashboard-health-audit-log.png) |

## 微服务远程链路更新（2026-09-02）

本表上方的历史汇总保留原有基线编号和当时的阶段性描述。当前实现已完成 `identity-service`、`merchant-service`、`order-service`、`assistant-service` 的独立运行；backend 在 `prod,remote` 下通过 HTTP internal contract 调用服务，业务事实分别落在 `life_assistant_identity`、`life_assistant_merchant`、`life_assistant_order`。Microservice E2E 已在 UC01～UC09 上以 9/9 PASS 完成，最新 controller、数据归属和跨服务调用口径见 [`MICROSERVICE_FINAL_ARCHITECTURE_MATRIX`](MICROSERVICE_FINAL_ARCHITECTURE_MATRIX.md)。

最终交付证据索引见 [`04_tests/evidence/README.md`](../04_tests/evidence/README.md)；其中集中链接最新 main CI、Microservice E2E、Kubernetes/ECS、性能原始结果和 HPA 远端 PASS/历史 BLOCKED 原始观测。

因此表中“待补”应按维度理解：它主要表示浏览器 UI 证据、单体基线的更细异常覆盖或人工演示项；不应再被解释为微服务远程主链路尚未建立。`DemoStore` 和 legacy `life_assistant` 仍是 compatibility/monolith、migration/backfill/rollback 能力，不是 `prod,remote` 的默认业务数据源。

## 结果口径

- “当前 `main@bc36f32` Maven `78/78`”是主线复验汇总，不表示每一行独占 78 个测试；每行的具体编号和验证点以测试报告及对应证据链接为准。
- 当前 `main@bc36f32` 本地前端 Vitest 为 `27/27` 通过；该数字是前端测试全集汇总，UC07 行同时列出其专项页面闭环结果。
- UC07 使用独立页面回归结果 `4/4` 和前端专项 `27/27`；UC08 使用独立 HTTP E2E 结果 `7/7`。
- 表中标记“待补”的内容保留为未完成项，不计入已通过结果。
