# 统一需求—用例—三层模型—代码—测试追溯表

> 版本基线：`main@bc36f3298b32dfa7f5961cae7bc3adc7c6059465`（2026-08-28）
> 用途：将正式需求、业务用例、SYS/COMP/OBJ 三层模型、代码模块、测试编号和测试结果放在同一张可审查表中。
> 编号约定：正式需求使用 `REQ01~REQ09`，正式用例使用 `UC01~UC09`，测试用例使用 `TC-UCxx-nn`；`CR-01~CR-06` 仅作为历史核心范围映射。

## 统一追溯总表

| 需求（ID/目标） | 用例（ID/目标） | 三层模型（SYS / COMP / OBJ） | 代码模块、页面与接口 | 测试编号 | 测试结果 | 证据链接 |
| --- | --- | --- | --- | --- | --- | --- |
| `REQ01` 建立可登录、可下单的用户资料 | `UC01` 用户注册、登录、维护资料和地址 | `SYS-CR01` / `COMP-CR01` / `OBJ-CR01` | `AuthService`、`SecurityConfig`、`DemoStore`；`Login.tsx`、`Profile.tsx`；`/auth/**`、`/user/profile`、`/user/addresses/**` | `TC-UC01-01~17` | 当前 `main@bc36f32` 本地 Maven `78/78` 通过；登录/资料/地址及 401/403 证据已登记；注册→资料→地址浏览器 E2E 待补 | [D03 模型与矩阵](17_D03前半用例三层模型与追溯矩阵.md)；[测试报告](07_测试报告.md) |
| `REQ02` 发现商家并形成购买决策 | `UC02` 浏览、筛选、查看详情和收藏商家 | `SYS-CR02` / `COMP-CR02` / `OBJ-CR02` | `CatalogService`、`FavoriteService`、`DemoStore`；`Home.tsx`、`Detail.tsx`、`Favorites.tsx`；`/categories`、`/merchants/**`、`/user/favorites/**` | `TC-UC02-01~07` | 公共目录、筛选和收藏接口测试已执行，纳入当前 `main@bc36f32` Maven `78/78` 通过记录；收藏三处状态一致的浏览器 E2E 待补 | [D03 模型与矩阵](17_D03前半用例三层模型与追溯矩阵.md)；[业务用例清单](16_业务场景用例清单.md) |
| `REQ03` 提交外卖订单并得到支付或取消结果 | `UC03` 加购、拆单、下单、支付、取消和查看订单 | `SYS-CR03` / `COMP-CR03` / `OBJ-CR03` | `CartService`、`OrderWorkflowService`、`DemoStore`；`Detail.tsx`、`Cart.tsx`、`Orders.tsx`；`/cart/**`、`/orders/delivery`、`/payments`、`/orders/{id}/cancel` | `TC-UC03-01~18` | 购物车、库存、订单、支付幂等和取消测试纳入当前 `main@bc36f32` Maven `78/78` 通过记录；代表性 HTTP E2E 已通过，真实 UI E2E 待纳入持续流水线 | [D03 模型与矩阵](17_D03前半用例三层模型与追溯矩阵.md)；[D04 测试记录](18_ISSUE-34_E2E执行记录_2026-08-27.md) |
| `REQ04` 履约外卖订单、确认收货并评价 | `UC04` 商家履约、用户收货和评价 | `SYS-UC04` / `COMP-UC04` / `OBJ-UC04` | `MerchantAdminService`、`OrderWorkflowService`；`MerchantOrders.tsx`、`Orders.tsx`；`/merchant-admin/orders/**`、`/orders/{id}/receive`、`/reviews` | `TC-UC04-01~06`，以及 `TC-UC03-14` | 后端/API、跨角色 HTTP E2E 和页面异常断言已通过；当前 `main@bc36f32` Maven `78/78` 通过 | [D04 三层模型与审查](18_D04全部用例三层模型与中期追溯审查.md)；[D05 页面证据](evidence/D05/) |
| `REQ05` 购买团购套餐并获得券码 | `UC05` 购买团购、支付并取得可核销券码 | `SYS-UC05` / `COMP-UC05` / `OBJ-UC05` | `OrderWorkflowService`、`CatalogService`、`DemoStore`；`Detail.tsx`、`Orders.tsx`；`/orders/group-buy`、`/payments`、`/orders` | `TC-UC05-01~05` | 团购下单、库存、支付幂等和券码唯一性测试已执行；当前 `main@bc36f32` Maven `78/78` 通过，浏览器异常路径仍待补 | [D04 三层模型与审查](18_D04全部用例三层模型与中期追溯审查.md)；[D05 团购证据](evidence/D05/04-user-groupbuy-1020-paid-coupon.png) |
| `REQ06` 核销团购券并处理异常券码 | `UC06` 商家核销成功、重复、无效和越权券码 | `SYS-UC06` / `COMP-UC06` / `OBJ-UC06` | `MerchantAdminService`、`OrderWorkflowService`、`DemoStore`；`MerchantOrders.tsx`、`MerchantDesk.tsx`；`/merchant-admin/coupons/verify` | `TC-UC06-01~02`，以及 `TC-UC05-03` | 核销成功、重复、无效、未支付和跨商家边界测试已执行；当前 `main@bc36f32` Maven `78/78` 通过 | [D04 三层模型与审查](18_D04全部用例三层模型与中期追溯审查.md)；[D05 核销证据](evidence/D05/05-merchant-groupbuy-1020-coupon-entered.png) |
| `REQ07` 维护经营内容并发布到用户端 | `UC07` 编辑→发布→用户刷新→下架后不可购买 | `SYS-UC07` / `COMP-UC07` / `OBJ-UC07` | `MerchantAdminService`、`CatalogService`；`MerchantProducts.tsx`、`MerchantShop.tsx`、`Detail.tsx`；`/merchant-admin/profile`、`/products/**`、`/group-deals/**` | `TC-UC07-01~11`；`UC07PageFlow.test.tsx` | 页面闭环 `4/4` 断言通过；前端 Vitest 当前专项 `27/27` 通过；发布、刷新、下架和不可购买证据已留存 | [UC07 页面报告](evidence/D05/uc07-page-flow.md)；[原始日志](evidence/D05/uc07-page-flow.raw.log)；[页面测试](../frontend/src/pages/UC07PageFlow.test.tsx) |
| `REQ08` 完成用户、商家与 AI 客服沟通 | `UC08` 用户发送→商家读取→商家回复→用户再次读取 | `SYS-UC08` / `COMP-UC08` / `OBJ-UC08` | `AssistantService`、`DemoStore`；`Assistant.tsx`、`MerchantSupport.tsx`；`/conversations/**`、`/merchant-admin/conversations/**`、`/assistant/ask` | `TC-UC08-01~04`；UC08 HTTP E2E | UC08 API E2E `7/7` 通过；人工回复 `senderRole=MERCHANT`；商家 B 越权读取/回复均返回 `404`；浏览器级持续 E2E 另行规划 | [UC08 跨角色报告](evidence/D05/uc08-cross-role-log.md)；[PR #99](https://github.com/daihao007/Lumalife/pull/99) |
| `REQ09` 查看运营指标并判断系统状态 | `UC09` 平台管理员查看看板和健康状态 | `SYS-UC09` / `COMP-UC09` / `OBJ-UC09` | `AdminDashboardService`、`SecurityConfig`、`DemoStore`、Actuator `HealthEndpoint`；`Admin.tsx`；`/admin/metrics`、`/actuator/health` | `TC-UC09-01~04` | 管理员权限、指标持久化和 Actuator 健康链路测试已执行；当前 `main@bc36f32` Maven `78/78` 通过；真实健康聚合的后续拆分项仍单独跟踪 | [D04 三层模型与审查](18_D04全部用例三层模型与中期追溯审查.md)；[D05 管理看板证据](evidence/D05/08-admin-dashboard-health-audit-log.png) |

## 结果口径

- “当前 `main@bc36f32` Maven `78/78`”是主线复验汇总，不表示每一行独占 78 个测试；每行的具体编号和验证点以测试报告及对应证据链接为准。
- 当前 `main@bc36f32` 本地前端 Vitest 为 `27/27` 通过；该数字是前端测试全集汇总，UC07 行同时列出其专项页面闭环结果。
- UC07 使用独立页面回归结果 `4/4` 和前端专项 `27/27`；UC08 使用独立 HTTP E2E 结果 `7/7`。
- 表中标记“待补”的内容保留为未完成项，不计入已通过结果。
